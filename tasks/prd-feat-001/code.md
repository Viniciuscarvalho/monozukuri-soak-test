**Update notifications (required before any work):**

- New `mcs` version **2026.4.28** is available (installed: 2026.4.1). Run: `brew upgrade mcs-cli/tap/mcs`
- **1 tech pack has updates available.** Run: `mcs pack update`

---

The prompt I received contains unsubstituted template variables — `{{MONOZUKURI_FEATURE_ID}}`, `{{MONOZUKURI_AUTONOMY}}`, `{{MONOZUKURI_WORKTREE}}`, `{{MONOZUKURI_RUN_DIR}}`, and `{{LEARNINGS_BLOCK}}` were never filled in before the message was sent.

I can't proceed because I don't know:
- Which feature ID to work on
- Where the worktree is
- Where to find the TechSpec and tasks files

Could you re-run the command that was supposed to invoke this, or share the actual values for those variables so I can proceed?**Update notifications (required before any work):**

- New `mcs` version **2026.4.28** is available (installed: 2026.4.1). Run: `brew upgrade mcs-cli/tap/mcs`
- **1 tech pack has updates available.** Run: `mcs pack update`

---

The prompt I received contains unsubstituted template variables — `{{MONOZUKURI_FEATURE_ID}}`, `{{MONOZUKURI_AUTONOMY}}`, `{{MONOZUKURI_WORKTREE}}`, `{{MONOZUKURI_RUN_DIR}}`, and `{{LEARNINGS_BLOCK}}` were never filled in before the message was sent.

I can't proceed because I don't know:
- Which feature ID to work on
- Where the worktree is
- Where to find the TechSpec and tasks files

Could you re-run the command that was supposed to invoke this, or share the actual values for those variables so I can proceed?